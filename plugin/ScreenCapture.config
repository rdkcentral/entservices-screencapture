set (autostart false)
set (preconditions Platform)
set (callsign "org.rdk.ScreenCapture")

if(PLUGIN_SCREENCAPTURE_STARTUPORDER)
set (startuporder ${PLUGIN_SCREENCAPTURE_STARTUPORDER})
endif()

map()
    key(root)
    map()
        kv(mode ${PLUGIN_SCREENCAPTURE_MODE})
        kv(locator lib${PLUGIN_IMPLEMENTATION}.so)
    end()
end()
ans(configuration)
